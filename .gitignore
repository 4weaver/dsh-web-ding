node_modules/
*.tsbuildinfo

# Build output. Regenerated by `npm run build` (tsdown); Nix's
# pkgs/dsh-web-ding builds it from source. Never commit it: a committed bundle
# is not byte-reproducible from this repo and would mask whether src/ still builds.
lib/
